// components/ui/ToastWrapper.tsx
"use client";
import { Toaster } from "react-hot-toast";

const ToastWrapper = () => {
  return <Toaster position="top-right" reverseOrder={false} />;
};

export default ToastWrapper;
